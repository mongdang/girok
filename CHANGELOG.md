# 변경 내역

배포되는 변경마다 `.claude-plugin/plugin.json` 의 버전을 올린다. 형식적인 규칙이 아니다 —
설치본은 버전 디렉터리에 캐시되므로 버전을 그대로 두면 이미 설치한 머신은 처음 받은 사본을
계속 쓰고, 마켓플레이스를 갱신해도 바뀌지 않는다. 이건 겪고 나서 알았다.

버전은 [semver](https://semver.org) 를 따른다. `1.0.0` 전까지는 minor 에서 설정 키가 바뀔 수 있다.

---

## 0.20.0 — 게이트 문언이 저장소를 따라 올라온다

`/notes` 의 sync 가 `<진행기록폴더>/CLAUDE.md` 의 부트스트랩 게이트를 이 판본의 문언으로
맞춘다. 도입은 이미 있는 파일을 덮어쓰지 않으므로, 0.19.0 이전에 도입한 저장소는 게이트가
그 시절 문언으로 영영 남았다 — 주입 블록이 없을 때 **"플러그인이 로드되지 않은 것이다"**
라고 진단하는 문언이다. 0.19.0 부터 사실이 아니다. 중단 규칙은 양쪽이 같아 안전 동작은
달라지지 않지만, 막힌 사람을 필요 없는 설치로 보낸다.

- 블록을 `<!-- girok:bootstrap:begin -->`·`<!-- girok:bootstrap:end -->` 로 감쌌다.
  다음 개정부터는 이 마커 사이만 바뀐다.
- 마커가 없는 저장소(0.19.0 이하)는 모양으로 찾는다 — `[girok] ready` 를 언급하는
  CAUTION 블록. 실장비 안전 게이트도 같은 CAUTION 이라, 그 문장이 둘을 가른다.
- **손으로 고쳐 쓴 게이트는 건드리지 않는다.** 알아보지 못하면 갱신을 포기하고 어느
  파일인지 말한다(`[확인]`). 문장 하나 고치자고 남의 규칙을 덮는 것은 잘못된 거래다.
- 블록 바깥은 전부 그대로다 — 프로젝트 고유 규칙도, 안전 게이트 절도.
- CRLF 로 보관된 파일은 CRLF 로 남는다. LF 로 다시 쓰면 모든 줄이 바뀐 것으로 보여
  정작 바뀐 한 곳이 묻힌다.

---

## 0.19.0 — 플러그인 없는 머신에서도 검사가 돈다

훅이 플러그인 안에만 있었다. 그래서 girok 이 들어간 저장소를 플러그인 없는 PC 에서 열면
세션 시작 블록이 뜨지 않았고, `CLAUDE.md` 게이트가 "플러그인이 로드되지 않았다 — 작업을
중단하라" 로 걸렸다. 규칙 전문은 저장소에 멀쩡히 커밋돼 있는데도 아무 작업도 할 수 없었다.

훅을 `.method/` 스냅샷으로 내리고, 등록을 저장소의 `.claude/settings.json` 으로 옮겼다.
플러그인은 훅을 **실행하는** 주체에서 훅의 **원본을 공급하는** 주체로 내려간다.

**변경**

- `.method/hooks/` 에 훅 12개가 함께 커밋된다. 콘텐츠 해시 안에 들어가므로 사람이 고친
  훅을 CI 가 잡는다. 검사기가 그랬듯 훅도 이제 저장소가 실제로 커밋한 코드다.
- 훅 등록은 `.claude/settings.json` 이 한다 — `${CLAUDE_PROJECT_DIR}` 아래 스냅샷 래퍼를
  가리킨다. 플러그인의 `hooks/hooks.json` 은 지웠다. 남겨 두면 두 등록이 모두 걸려 모든
  검사가 두 번 돈다(Claude Code 는 플러그인 훅과 프로젝트 훅을 별개로 유지하고, 명령
  문자열이 다르면 중복 제거가 걸리지 않는다).
- 등록은 `sync` 가 스냅샷을 쓰는 자리에서 함께 한다. 둘로 나누면 훅은 커밋돼 있는데
  아무도 등록하지 않은 저장소가 생길 수 있고, 그 상태는 정상과 겉모습이 같다.
- `verify` 가 등록을 확인한다. `.claude/settings.json` 은 `.method/` 밖이라 해시가 지키지
  못하는데, 세션 안에서 그걸 잡는 ready 마커 자체가 훅에서 나오기 때문이다. 이벤트별로
  확인하므로 저장소가 자기 훅을 더 등록하는 것은 막지 않는다.

**고친 것**

- `plugin_version()` 이 매니페스트를 찾다 `FileNotFoundError` 를 냈고 세션 시작 보고가
  그걸 잡지 않았다. 완전히 설정된 저장소가 플러그인 없는 PC 에서 **아무것도 보고하지
  못했다.** ready 마커는 이제 스냅샷 판본을 말한다 — 그 마커가 증명하는 것은 "이 저장소에
  규칙이 있다" 이지 "이 머신에 플러그인이 깔렸다" 가 아니다.
- `hooks/run-hook.cmd` 가 `100644` 로 커밋돼 있었다. Linux·macOS 에서 플러그인을 설치하면
  bash 가 실행 권한 없는 래퍼를 실행하지 못한다 — 등록은 되어 있고 아무것도 돌지 않는,
  이 판본이 없애려는 바로 그 상태다. `100755` 로 올렸고, 스냅샷 사본도 파일 권한과 git
  인덱스 모드를 함께 세운다.
- 게이트 문언. 주입 블록이 없다는 것이 이제 "플러그인이 없다" 를 뜻하지 않는다. 셋 중
  하나다 — 폴더 신뢰 미승인, Python 없음, 스냅샷이 낡아 등록되지 않음. 중단 규칙 자체는
  그대로다. 오히려 정확해졌다: 전에는 멀쩡한 머신에서도 걸렸고, 이제는 검사가 실제로
  돌지 않을 때만 걸린다.
- **플러그인 없이 `sync` 를 돌리면 거부한다.** `PLUGIN_ROOT` 는 기본값이 스크립트의 조부모
  폴더인데, 스냅샷 사본에서는 그게 `.method/` 자신이다. sync 는 그 폴더를 지우고 다시 쓰므로,
  플러그인 없는 머신에서 스냅샷의 `method_sync.py` 를 직접 돌리면 **폴더를 지운 뒤 지워진
  자리에서 원본을 찾다가** 훅 12개와 검사기 5개를 전부 잃고 "완료" 를 찍었다. 이제 아무것도
  지우기 전에 확인하고 멈춘다.
- **저장소가 등록해 둔 다른 훅을 지우지 않는다.** `verify` 는 저장소가 자기 훅을 더 넣는
  것을 허용하는데 `sync` 는 `hooks` 블록을 통째로 갈아끼웠다 — 설계의 두 절반이 어긋나
  있었고, 업그레이드 후 첫 `/notes` 가 남의 훅을 조용히 멈췄을 것이다. 이제 girok 이 쓴
  항목만 교체한다.
- **알아볼 수 없는 `VERSION` 을 판본으로 읽지 않는다.** 스탬프의 마지막 낱말을 판본으로
  삼았기 때문에, `corrupt data` 가 든 파일이 `ready vdata` 를 냈다 — 아무도 확인하지 않은
  스냅샷을 게이트가 받아들이는 상태다. 64자리 해시가 없으면 "스냅샷 없음" 으로 본다.
- 등록 확인이 경로를 부분일치로 봤다. `vendor.notes/.method/hooks/run-hook.cmd` 가
  `notes/.method/...` 로 통과했다 — 파일에는 맞게 적혀 있고 훅은 돌지 않는, 이 검사가
  잡으려던 바로 그 경우다.
- 손상된 `.claude/settings.json` 을 만나면 조용히 넘어가지 않는다. 덮어쓰지 않는 것은
  그대로지만, 훅이 등록되지 않았다는 사실을 말한다. 도입 명령의 종료 코드도 이때 0 이
  아니다 — 뼈대는 섰는데 아무것도 검사하지 않는 상태를 성공으로 보고했었다.
- **sync 가 중간에 실패해도 쓰던 스냅샷이 남는다.** 폴더를 먼저 지우고 스무 개 파일을
  써 넣었기 때문에, 도중에 실패하면 반쯤 지어진 `.method/` 가 남고 다음 실행은 그걸
  남의 폴더로 보고 거부했다. 이제 옆에 지어 다 되면 바꿔 낀다.

> [!IMPORTANT]
> **이 판본으로 올린 뒤 각 저장소에서 `/notes` 를 한 번 돌려야 훅이 다시 붙는다.**
> 플러그인 쪽 등록이 사라졌고 저장소 쪽 등록은 sync 가 만들기 때문이다. 그 전까지는 세션
> 시작 블록이 뜨지 않고 게이트가 작업을 멈춘다 — 검사가 실제로 돌지 않는 상태이므로 그게
> 옳은 동작이다.

**남는 한계**

- Python 이 없는 머신에서는 여전히 훅이 돌지 않는다. 규칙 전문과 CI 만 남고 사전 차단은 없다.
- 저장소를 새로 도입하는 일과 규칙 개정을 내려받는 일에는 플러그인이 필요하다. `templates/`
  와 `skills/` 원본을 읽어야 하고, 그건 스냅샷에 넣지 않는다 — 넣으면 규칙 원본이
  저장소마다 갈라진다.

## 0.18.1 — 첫 실전 도입에서 나온 두 가지

0.18.0 을 실제 저장소(`eq-agent-v4`)에 처음 적용하면서 드러난 것 둘을 고쳤다.
둘 다 이식 기능과는 무관한, 그 전부터 있던 문제다.

**변경**

- 병행 작업이 꺼진 저장소에 개인 폴더를 만들지 않는다. 초기화가 `--worker` 만 보고
  `docs_<id>/` 를 만들어서, `parallelMode: false` 인데 `workers` 항목이 있는 저장소는
  아무도 병합하지 않는 현황판 한 벌을 받고 손으로 지워야 했다.
- 다른 저장소의 결정을 인용할 때 조사가 떨어져 있으면 그 사실을 말한다. `` `저장소명` 의
  decisions/011 `` 처럼 띄어 쓰면 검사기가 "존재하지 않는 011 인용" 으로만 보고했다.
  원인을 안 알려주니 한 사람은 검사기 버그를 찾아 나섰고, 다른 사람은 통과시키려고
  조사를 지웠다 — 뜻이 상한다. 조사는 앞말에 붙여 쓰는 것이 맞고, 검사기가 그걸
  전제한 것도 맞다. 메시지만 고쳤다.

## 0.18.0 — 기존 기록의 인수인계

`/notes` 는 뼈대만 만들고 이미 있던 문서는 그 자리에 두었다. 진짜 문서 옆에 빈 문서
한 벌이 남았고, 정리는 사람 몫이었다.

`notes_adopt.py` 를 더했다. `backup` 이 원본을 통째로 남기고, `plan` 이 전수 목록과
분류 초안을 만들고, `apply` 가 `git mv` 로 옮기고 링크를 다시 걸고, `verify` 가
바이트를 다시 읽어 유실이 없음을 증명한다.

- 흐름은 `plan` → 사람이 `?` 로 남은 role 을 채움 → `apply` 로 고정했다. `apply` 는
  매핑이 없으면 스스로 만들지 않고 거부한다 — 승인 없이 문서가 움직이는 경로를 남기지
  않기 위해서다.
- git 이 없는 폴더는 거부하지 않는다. `git init` 과 `.gitignore` 정비를 이식의
  일부로 수행하고, 첫 커밋에서 뺀 것(비밀·대용량)을 기록한다. 이미 커밋된 파일은
  `.gitignore` 로 못 뺀다는 것도 안내한다 — 답은 이력 재작성이 아니라 비밀 폐기·교체다.
- `apply` 는 복원 태그(`girok-adopt-before-<날짜>`)를 남기고 문서가 옮겨간 자리에 맞춰
  `.claude/girok.json` 을 고친다. 커밋은 하나가 아니다 — 이미 git 이 있던 저장소에서는
  `.gitignore` 정비 커밋이 먼저 생기고 이식 커밋이 뒤에 온다. 중간에 막혀도(이름 충돌,
  자기 자신 병합 등) 이 시점부터는 안전망이 이미 있으므로, 실패 안내가 실제 태그·백업
  폴더 이름으로 복원 방법을 알려준다. 복원은 백업 폴더 통째가 1순위다 — 태그로 옛 경로만
  되살리면 새 경로의 사본이 함께 남아 문서가 둘로 갈린다.
- 병합은 이어붙이기만 한다. 재작성은 결과가 깔끔한 대신 유실을 검증할 수 없다.
- `notes_config.is_workspace()` 를 더했다. 자동 `git init` 이 상위 폴더에서 돌면
  하위 저장소를 통째로 삼키므로, 그것만은 중단한다. 기존 `is_repository` 는
  건드리지 않았다 — 세션 훅과 검사기가 함께 쓰는 판정이다.

## 0.17.0

**저장소 전체 정밀 점검 — 복사된 규칙이 갈라진 자리, 검사가 평범한 작업을 막던 자리.**

이 프로젝트가 없애려는 문제가 이 프로젝트 안에서도 일어나 있었다: 규칙을 한 곳에 두는
대신 복사해 뒀고, 복사본이 갈라졌다.

| 무엇이 깨져 있었나 | 결과 |
|---|---|
| 게이트 행 정규식이 훅과 검사기에 각각 복사돼 있었고, 검사기만 하위 항목(`3a`)을 알게 개정됐다 | **OPEN 인 `3a` 가 열린 항목 0건으로 읽혔다 — 모션 명령이 그대로 실행됐다** |
| force push 스캔이 `git push` 부터 줄 끝까지 훑었다 | 뒤에 이어 붙인 다른 명령의 `-f` 를 push 플래그로 읽고 평범한 명령을 차단했다 |
| `## 목차` 절을 빈 줄 + `---`/`##` 로만 찾았다 | 목차가 문서 마지막 절이면 **앵커 검사를 통째로 건너뛰고**, 있는 목차를 없다고 경고했다 |
| 열린 항목을 줄 전체의 `OPEN` 문자열로 셌다 | 항목 설명에 그 단어가 있으면 닫힌 항목이 열린 것으로 읽혔다 |
| `CLAUDE.md` 포인터가 기본 레이아웃 경로를 박아 썼다 | 평평한 저장소에 **없는 `docs/PROGRESS.md` 를 가리키는 포인터**가 생성됐다 — 경로를 알려주는 게 일인 문서가 |
| push 의 dry-run | "커밋이 원격에 올라갔다" 로 보고됐다 |
| `docRoots: []` 같은 반쯤 고친 설정 | 모든 검사가 IndexError 로 죽었다 |
| git 이 PATH 에 없는 머신 | PreToolUse 훅이 도구 호출마다 실패를 뱉었다 |
| 스냅샷에서 `verify` 할 때(플러그인 없음) 해시 불일치 | 원인 파일로 **항상 `RULES.md`** 를 지목했다 |
| 빈 `VERSION` 파일 | 세션 시작 검사가 예외로 끝났다 |

고친 것:

- **게이트 행 규칙을 한 곳으로 합쳤다.** 훅은 검사기의 정의를 가져다 쓴다 — 절대경로
  정규식이 이미 그렇게 돼 있었는데 게이트 행만 복사본이었다.
- 열린 항목은 **상태 열에서** 센다. 헤더를 못 읽는 표에서는 줄 전체 스캔으로 되돌아간다 —
  많이 세면 모션이 막히고 적게 세면 통과한다. 틀려도 되는 방향은 하나뿐이다.
- force push 와 dry-run 판정은 **그 명령의 구간만** 본다(`&&`·`;`·`|` 로 끊는다).
- 포인터 문서(`CLAUDE.md`·`AGENTS.md`·`GEMINI.md`)와 `SAFETY_GATE.md` 의 경로가 설정에서
  나온다. 아카이브 모듈이 꺼져 있으면 아카이브 폴더를 안내하지 않는다.
- 스캔이 `.git`·`node_modules` 에 **실제로 안 들어간다**(`rglob` → `os.walk` 가지치기).
  전체 검사는 문서를 한 번만 읽는다. 반입 스캔은 최근 12개 브랜치까지만 보고, 남은 개수를
  **말한다** — "아무것도 안 왔다" 와 "12개까지만 봤다" 는 다른 문장이다.
- 문서 오기: `workers` 매핑 방향(id → 이메일), 설정 표의 `remote` 누락, 목차의 라이선스
  누락, 사용법의 force push 탈출구 누락. `writing-adr` 이 "인덱스 상태 열이 어긋나면
  검사기가 잡는다" 고 적어 뒀는데 **검사기는 그걸 보지 않는다** — 안 하는 일을 한다고
  적어 두면 아무도 확인하지 않게 된다.

302 tests.

## 0.16.0

**force push 가드에 구멍이 있었고, 그 구멍으로 뚫었다.**

가드는 `--force`·`--force-with-lease`·`-f` 만 봤다. **refspec 형태(`git push origin +master`)
는 같은 작업인데 플래그가 없어서 그냥 통과했다.** 사용자가 이력 리셋을 지시한 날, 그
형태로 가드를 우회해 push 했다 — 만든 사람만 아는 구멍은 없는 가드보다 나쁘다. 다른
사람은 가드를 믿기 때문이다.

- refspec 형태도 차단한다. `+` 는 인자 첫 글자일 때만 세므로 `feature+extra` 브랜치는
  평범한 push 다.
- **탈출구를 뒀다 — 스위치가 아니라 이유다.** `GIROK_FORCE_PUSH_REASON` 에 8자 이상의
  이유를 담으면 통과하고, 훅이 그 이유를 세션에 그대로 남긴다. 규칙을 어길 수 있게 하되
  어긴 사실과 근거가 기록에 남는 쪽을 택했다 — 막기만 하면 결국 우회되고, 우회는 기록을
  남기지 않는다.
- 차단 메시지가 두 갈래를 다 알려준다: 트리 불변 커밋(`-s ours` 조상 연결)과 위 탈출구.

288 tests.

## 0.15.0

**이름이 girok 이 됐고, 옛 이름은 이 저장소 어디에도 남지 않는다.** 옛 이름의 접두어가
개인정보성 식별자라 이름·주소·이력에서 전부 걷어냈다:

| 항목 | 이후 |
|---|---|
| GitHub 저장소 | `mongdang/girok` — 새 저장소. 옛 저장소는 삭제(리다이렉트 없음), 이력도 새 initial commit 으로 리셋 |
| 플러그인 | `girok@mongdang` — 기존 설치자는 **재설치 필요** |
| 설정 파일 | `.claude/girok.json` |
| 세션 마커 | `[girok] ready` |

이미 도입한 저장소가 깨지지 않게 두 가지를 계속 받아들인다: 개명 전 설정 파일명
(`notes-method.json`)은 그대로 읽고(새 이름이 있으면 새 이름이 이긴다), 개명 전 이름으로
자기를 밝히는 `.method/` 스냅샷도 **이름이 아니라 VERSION 스탬프의 형식(64자리 해시)으로**
알아보고 재동기화한다 — 옛 이름을 코드에 다시 적지 않기 위해서다. 두 경로 다 테스트로
고정했다.

## 0.14.0

**payload 필드명을 셋 다 틀리게 쓰고 있었고, 테스트가 같은 오해를 검증하고 있었다.**

초록불이 아무 의미가 없던 사례다. 각 훅이 자기 필드명을 적었고, 각 테스트가 그 훅에
동의했다.

| 잘못 쓴 것 | 실제 | 그래서 무슨 일이 |
|---|---|---|
| `user_input` | `prompt` | **실장비 모션 언급 시 안전 게이트 주입이 한 번도 발동하지 않았다** |
| `tool_result` | `tool_response` | 실패한 push 를 "완료" 로 보고했다 |
| `Path(root)` (미해결) | `.resolve()` | `--root .` 로 실행하면 설정 있는 저장소에서 죽었다 |

근거는 같은 이벤트를 읽는 다른 플러그인들이다 — `claude-plugins-official/security-guidance`
가 `tool_response` 에서 `stdout`·`stderr`·`interrupted` 를 꺼내 쓰고(Bash 에는 exit code 가
없다), `caveman` 이 `event.prompt ?? event.user_prompt ?? event.userMessage` 를 읽는다.

세 곳을 고치는 것으로 끝내지 않았다:

- **필드 접근이 `hook_io` 한 곳으로 모였다** — `prompt_of` · `output_text_of` · `failed`.
  실제 이름을 그 근거와 함께 적어두고, 옛 이름은 폴백으로 남긴다(여기서 틀리면 조용하고,
  폴백은 비용이 없다).
- **훅이 payload 를 직접 읽으면 테스트가 실패한다.** 필드명이 셋이나 동시에 틀린 이유가
  "훅마다 자기 이름을 적었다"는 것이었다. 접근자 하나면 틀릴 곳도 고칠 곳도 하나다.
- 실패 판정이 `interrupted`·`is_error`·출력 텍스트를 함께 본다. 응답이 없으면 실패로
  치지 않는다 — 침묵은 증거가 아니다.

`notes_init` 은 `--root` 를 resolve 하고, 문서 폴더 이름을 설정에서 읽는다(`docs` 하드코딩
제거). 이 세 버그와 문구 다듬기는 **다른 세션에서 찾아 고친 것**이고, 이 릴리스는 그 위에
구조적 방지책을 얹은 것이다.

**세 번째 저장소(eq-agent-v3) 실전 도입에서 나온 것.**

- `skipDirs` 설정 — 검사기가 건너뛸 폴더를 저장소가 지정한다. 규칙 도입 전에 쓰인 동결
  산출물(계획 트랜스크립트 등)을 검사하면 진짜 발견이 묻힌다.
- `limits.tocMinKB` — 목차를 요구하는 최소 문서 크기. 자체 8KB 상한을 가진 저장소가
  3KB 메모마다 목차 경고를 받지 않게 한다.
- numbered 스타일에도 타 저장소 인용 형식이 생겼다 — `` `저장소명`의 decisions/NNN ``.
  그전에는 v2 결정을 인용한 문장이 전부 "존재하지 않는 인용"으로 신고됐다.
- 뼈대 템플릿(현황판·결정 인덱스)이 기본 레이아웃을 하드코딩하지 않는다 — 현황판 경로,
  아카이브 유무, ADR ID 규칙이 설정을 따라간다. 평면 레이아웃 저장소가 "현황판은
  docs/PROGRESS.md" 라고 적힌 인덱스를 받고 있었다.
- **설정 파일이 없는 폴더에서는 작업자 차단을 걸지 않는다.** `parallelMode` 기본값이
  true 라서, 방법론을 도입한 적 없는 저장소나 저장소 밖에서 켠 세션의 `git commit` 이
  "작업자 미확정"으로 막혔다 — 아무도 채우라고 요청받은 적 없는 설정 때문에. 도입
  커밋 자체가 이것에 막히면서 발견됐다.

264 tests.

## 0.13.1

**훅 두 개가 실제 Claude Code payload 필드명을 읽지 않고 있었다.**

테스트 242개가 전부 통과하는 상태에서 나왔다 — 픽스처가 훅과 같은 잘못된 이름을 쓰고
있어서, 자기들끼리만 맞는 초록불이었다. 이 프로젝트가 경계하는 바로 그 실패다.

- `UserPromptSubmit` 훅이 `user_input` 을 읽었는데 Claude Code 는 `prompt` 로 보낸다.
  **모션 언급 시 게이트 알림이 실환경에서 한 번도 발화한 적이 없다.** 이제 `prompt` 를
  읽는다(`user_input` 은 예비로 남긴다).
- `PostToolUse` 훅이 `tool_result` 를 읽었는데 실제 필드는 `tool_response` 다. 실패
  신호를 항상 못 보니 **실패한 push 도 "완료"로 보고됐다** — 0.10.0 이 정확히 막으려던
  동작이다. dict 응답의 `stdout` 도 실패 문구 탐색에 포함했다.

**적혀 있었지만 존재하지 않던 것들.**

- `parallel-docs` 규칙이 실행하라던 `merge_overlap.py` 는 **구현된 적이 없다.** 규칙만
  있고 도구가 없었으므로, 같은 결과를 내는 git 명령(merge-base 후 양쪽 `diff --name-only`
  교집합)으로 규칙을 고쳤다.
- 결정 인덱스 템플릿이 `templates/ADR.md` 를 참고하라고 했는데, 도입된 저장소에는 그
  경로가 없다(플러그인 저장소에만 있다). 저장소 안에 실재하는 `.method/RULES.md` 의
  ADR 카드 구조를 가리키게 했다.

**초기화가 `docs` 를 하드코딩했다.**

- `notes_init` 이 안전 게이트·아카이브·현황판 기본 경로를 설정의 `docRoots` 첫 항목이
  아니라 문자 그대로 `docs/` 에 만들었다. 첫 doc root 가 다른 이름인 저장소에서는
  검사기가 보지 않는 폴더에 게이트가 생겼을 것이다. 이제 설정을 따른다.

그 외: 스탬프 정규식 주석이 엉뚱한 정의(`PUSH_RE`) 위에 있던 것, pyproject 버전이 배포
버전으로 오독될 수 있던 것(주석으로 명시), 테스트의 raw string 누락(Python 3.12
SyntaxWarning).

**문장 정비.** README 머리글을 포함해 번역체·어색한 문장을 다듬고 말투를 통일했다.
GitHub About 과 플러그인 매니페스트 설명도 한글로 바꿨다(이 저장소의 문서는 한국어가
기본이다). 문서 속 세션 예시의 버전 표기는 `vX.Y.Z` 로 두어 릴리스마다 낡지 않게 했다.

## 0.13.0

- 조사가 **이미 도입한 저장소에서도 쓸모 있게** 됐다. 그전에는 경로만 비교했다. 이제 모듈
  판단이 저장소 모습과 어긋나면 알려준다 — 도입 시점에 맞았던 판단이 계속 맞는 것은
  아니다. 연구 트랙이 하드웨어 제어로 자라도 안전 모듈 플래그를 다시 보는 사람은 없다.
- `notes_survey.py` 가 `.method/` 스냅샷에 포함된다. 플러그인 없이도 "내 구조가 어긋났나"
  를 물을 수 있다 — 검사기를 스냅샷에 넣은 것과 같은 이유다.
- 사용법 문서에 조사 절 추가.

## 0.12.1

- 유출 스캔이 테스트 픽스처의 `a@x.invalid` 를 사내 주소로 신고했다. RFC 6761 이 `.invalid`
  를 정확히 이 용도로 예약해 뒀으니 — 절대 resolve 되지 않으므로 실제 주소일 수 없다 —
  모든 `.invalid` 도메인을 면제한다. 카나리아는 그대로 잡힌다.

## 0.12.0

**도입 전에 저장소를 읽는다.**

그전까지 `/notes` 는 기본값으로 뼈대를 만들고, 이미 있던 것은 그냥 옆에 남겼다. 두 번째
저장소에 맞는 레이아웃을 고른 건 **사람이 저장소를 읽고 판단한 것**이었다 — 플러그인은
그걸 못 했으므로, 자기 관례를 가진 프로젝트에 도입하면 진짜 문서 옆에 빈 문서 한 벌이
더 생겼을 것이다.

`notes_survey.py` 가 조사해 **제안**한다. 아무것도 쓰지 않는다.

| 추론 | 근거 |
|---|---|
| 진행기록 위치 | `docs/` 와 자기 규칙 파일을 함께 가진 폴더, 없으면 저장소 루트 |
| 현황판 | 이름과 절 구성(일자별 로그·활성 위험·한 줄 …). 후보가 둘 이상이면 고른 것과 나머지를 함께 보고 |
| 결정 기록·형식 | `decisions`·`adr` 폴더의 파일명에서 `ADR-NNN` / `NNN-` 판별 |
| 안전 게이트 | 게이트 문서, 코드의 안전 마커, 인터락·모션 표현 |
| 아카이브 | 기존 폴더. 단 **"아카이브 폴더를 만들지 않는다"고 적어둔 저장소는 그 관례를 따른다** |
| 병행 작업 | `docs_<id>/` 폴더 |

보고하는 것: 인덱스 없는 결정 폴더, 결정 폴더 밖에 떠도는 ADR, 크기를 넘긴 문서,
등재되지 않은 안전 마커, 현황판을 못 찾은 경우.

실제 저장소 두 곳에 돌려 **오탐 3건을 잡았다** — 이건 픽스처로는 안 나왔다:

- 병행 기간의 `docs_<id>/decisions/` ADR 을 "떠돌이"로 신고했다. 규칙상 정상 위치인데
  첫 실행에서 8건을 잘못 지적했다.
- `041-screen-shows-state-we-were-not-reading.md` 를 이름에 "state" 가 있다는 이유로
  현황판 후보로 올렸다. 결정 폴더는 후보에서 뺀다.
- 커밋 이메일 4개를 사람 4명으로 읽고 병행 작업을 켰다. 한 사람이 회사·맥북·Colab 을 쓰면
  그렇게 된다 — 이제 이메일 수는 **결정이 아니라 질문**이고, 병행 작업은 작업자 폴더로만
  판단한다.

## 0.11.0

**적혀 있었지만 아무도 검사하지 않던 규칙 5개.**

규칙을 문서에 적고 검사하지 않으면 어긋난다 — 이 프로젝트가 없애려는 문제인데, 자기 검사기
안에서 그걸 다섯 개 찾았다.

| 새 검사 | 등급 | 왜 |
|---|---|---|
| `## 목차` 없음 | 경고 | 규칙이 요구하는데 검사기는 "목차 없음(건너뜀)" 만 찍고 넘어갔다. 결정 카드와 짧은 메모는 예외 |
| 문서가 `---` 로 시작 | 실패 | GitHub 이 Jekyll front matter 로 오인해 렌더링이 통째로 깨진다 |
| CLOSED 게이트 항목에 확인자 없음 | 실패 | **확인되지 않은 항목이 확인된 것으로 읽힌다.** 이 설계가 존재하는 이유 |
| CLOSED 게이트 항목에 날짜 없음 | 실패 | 언제 확인했는지 되짚을 수 없다 |
| 단독 캐리지 리턴 | 실패 | 줄이 쪼개져 표가 끊긴다. **편집기에 보이지 않는다** |

마지막 것은 실제 문서에서 **두 번** 나온 결함이다 — 둘 다 표 셀 안 Windows 경로에서
백슬래시-r 이 실제 제어문자로 들어갔다. 앞의 것은 눈으로 찾았고, 검사를 넣자마자 두 번째가
바로 나왔다.

## 0.10.0

**push 가 "알아서 되는" 절반이 빠져 있었다.**

원본 설정은 자동 push 가 아니었다 — `Bash(git push:*)` 를 권한 allowlist 에 넣어 **승인
클릭을 없애고**, push 가 실제로 나갔는지 한 줄 찍어주는 것이었다. 그래서 알아서 되는 것처럼
느껴진 것이다. 플러그인에는 둘 다 없었다.

- `templates/settings.json` 이 `permissions.allow` 에 `Bash(git push:*)` ·
  `PowerShell(git push:*)` 를 넣는다. 저장소에 커밋되므로 팀원 전원에게 적용된다.
  `--force` push 는 PreToolUse 가 차단하고 **deny 가 allow 를 이긴다** — 원본보다 안전하다.
- PostToolUse 가 push 결과를 확인한다. 실패한 push 를 성공으로 보고하지 않는다 —
  그러면 커밋이 한 머신에만 남은 채 모두가 공유됐다고 믿게 된다.

**절대경로 규칙을 검사기가 보게 됐다.**

- 그전에는 편집 시점 훅만 잡았다. 플러그인이 없는 사람, 웹 편집, 도입 전에 쓰인 파일로
  들어온 절대경로는 아무도 못 잡았다. 이제 검사기가 경고한다(차단이 아니라 경고 — 훅과
  같은 등급).
- 정규식이 공백과 유니코드 `\w` 를 포함해서 **뒤따르는 한글 문장까지 경로로 먹었다.**
  ASCII 경로 문자로 제한했다. 레지스트리 키(`HKCU\...`)는 제외한다 — 그 키 자체가 값이다.
- 같은 정규식이 훅과 검사기에 **두 벌 있었다.** 사본이 어긋나는 게 이 프로젝트가 없애려는
  문제라서 하나로 합쳤다.
- 이 저장소 자신의 문서에 있던 절대경로 2건도 제거했다.

## 0.9.0

**반입 스캔이 실제로 구현됐다.**

- 병행 작업 규칙 전체가 "상대가 뭔가 push 했다는 걸 알아차리는 것"에서 시작하는데, 그
  알아차림이 구현돼 있지 않았다. `git` 명령이 5개뿐이었고 `fetch` 는 없었다 — 규칙만
  문서에 있고 방아쇠가 없었던 셈이다.
- 이제 세션 시작에 `git fetch` 후 상대 브랜치의 새 커밋·바뀐 문서·기본 브랜치 진행을
  보고한다. **안전 게이트 변경이 섞여 있으면 따로 표시한다** — 병합 순서가 안전부터
  시작하기 때문이다.
- 전부 읽기 전용이다. `fetch` 는 원격 추적 ref 만 움직인다. 병합·체크아웃·작업 트리
  쓰기는 없고, 병합 여부는 사람이 승인한다.
- 오프라인·인증 실패는 경고로 남기고 나머지 검사를 계속한다. fetch 는 12초에서 끊는다 —
  느린 원격 때문에 세션이 멈춘 것처럼 보이면 안 된다.
- git 없는 폴더 안내가 두 경우를 다 말한다: 한 단계 위에서 켰거나, 아직 git 을 안 쓰는
  프로젝트다(후자는 `git init` 이 답이다 — 이 방법론은 판본 추적·병합·커밋 즉시 push 를
  git 에 의존한다).
- 2인 시나리오를 실제 bare 원격 + 클론 두 개로 전 구간 테스트했다.

## 0.8.0

**저장소가 아닌 폴더에서 켠 경우를 알아본다.**

- Claude Code 에는 저장소 선택 화면이 없다 — 켠 폴더가 대상이다. 그래서 한 단계 위에서
  켜는 건 흔한 실수인데, 그 경우 진행기록 폴더를 **옆에 있는 다른 저장소**로 추정했다.
  실제로 여러 저장소를 담은 상위 폴더에서 켜니 옆에 있던 플러그인 저장소를 골랐다 — 즉
  플러그인 저장소 안에 다른 프로젝트의 스냅샷을 만들 수 있었다.
- 이제 `.git` 도 설정도 없는 폴더는 저장소가 아니라고 판정한다. 초기화는 거부하고, 세션은
  "여기는 저장소가 아니다, 한 단계 위에서 켠 것이 아닌지 확인할 것" 이라고 말하고,
  검사기는 0개 문서를 검사하고 통과하는 대신 실패한다.
- `docs/` 가 있는 하위 폴더도 그것이 **자기 저장소라면 채택하지 않는다.** 여러 체크아웃이
  한 폴더에 있을 때 남의 프로젝트 문서를 조용히 집는 경로였다.
- 하위 폴더에서 켜는 것은 그대로 동작한다(위로 올라가 저장소 루트를 찾는다).

## 0.7.0

- 초기화가 `--worker <id>` 를 받으면 `workers` 와 `mergeOwner` 까지 채운다. 그전에는
  `docs_<id>/` 를 만들면서 매핑은 비워둬서, **초기화 직후 첫 쓰기가 곧바로 "작업자 미확정"
  으로 막혔다** — `/notes` 가 저장소를 세팅한 다음 거기에 쓰기를 거부하는 셈이었다.
  이제 초기화 직후 세션이 질문 없이 작업자를 확정한다.

## 0.6.2

- `notesDir: "."` 로 초기화하면 저장소 루트에 "이 폴더는 문서 전용이다" 라는 `.gitignore` 를
  만들었다. 코드가 있는 루트에서는 그냥 틀린 말이고, 그 프로젝트의 산출물을 무시하기
  시작했을 것이다. 진행기록이 자기 폴더를 가질 때만 만든다.

## 0.6.1

- 재검토 정리: `**kwargs` 로 인자를 우회하던 두 곳(`gate_rules.decide`,
  `session_close.check`)을 제대로 된 인자로, 중복된 git 이메일 조회 헬퍼를 하나로.
- `templates/girok.json` 이 이제 설정 키 15개를 전부 적어둔다 — 신규 저장소에서도
  무엇을 바꿀 수 있는지 파일만 열면 보인다.
- 문서에 남아 있던 옛 버전 예시와, 새로 강제되는 동작(작업자 확정·`--confirm`)을 반영.

## 0.6.0

**훅이 payload 를 잘못 읽고도 자신 있게 답하던 것을 고쳤다.**

- `read_payload` 가 JSON 파싱 실패를 삼키고 빈 dict 를 돌려줬다. 그러면 `cwd_of` 가 `"."` 로
  넘어가 **다른 저장소를 대상으로** 보고했다 — 스냅샷이 멀쩡히 있는 저장소에 "스냅샷이
  없다"고 답했다. 이제 실패는 예외로 크게 드러나고, cwd 가 없으면 추측하지 않는다.
  틀린 답을 주는 검사는 실패하는 검사보다 나쁘다. 출력만 봐서는 틀린 줄 알 수 없기 때문이다.
- **stdin 을 로케일 인코딩으로 읽고 있었다.** 한국어 Windows 라면 cp949 다. Claude Code 는
  JSON 을 ASCII 로 이스케이프하지 않으므로, 한글이 담긴 프롬프트나 경로가 오면 깨지거나
  예외가 났을 것이다. 그전까지 통과한 테스트는 전부 `json.dumps` 의 기본 이스케이프 덕이었다.
  이제 바이트를 직접 `utf-8-sig` 로 디코드한다(BOM 도 같이 처리된다).
- `hooks.json` 에서 `"shell": "bash"` 를 뺐다. Claude Code 는 shell 을 생략하면 bash 를,
  Git Bash 가 없는 Windows 에서는 PowerShell 을 쓴다. **세 경로(bash · cmd.exe · PowerShell)
  전부 실측**했으므로 이제 Git Bash 없이도 돈다 — "Python 과 git 만 필요하다"는 문장이
  드디어 사실이 됐다.
- `notes_init` 이 `--confirm <저장소 폴더 이름>` 을 요구한다. 참고 저장소나 "손대지 마라"고
  합의된 저장소는 그 사실이 저장소 안에 적혀 있지 않다. 실제로 그런 지시를 받은 저장소가
  명령 한 번에 초기화될 수 있는 상태였다.
- **스탬프 갱신이 문서 내용을 지우고 있었다.** 정규식 끝의 `\s*$` 가 개행까지 삼켜서,
  스탬프 **다음 빈 줄이 갱신마다 사라졌다.** 충분히 반복되면 스탬프가 뒤 블록에 붙는다.
  이제 수평 공백만 매칭한다. 같은 함수가 CRLF 파일을 LF 로 바꿔 한 줄 변경을 파일 전체
  diff 로 만들던 것도 고쳤다 — 파일의 줄바꿈 방식을 그대로 유지한다.
- `sync` 가 `.method/` 를 지우고 다시 만드는데, **그 폴더가 이 플러그인이 만든 것인지
  확인하지 않았다.** 다른 용도로 같은 경로를 쓰는 저장소는 그걸 잃었을 것이다. 이제
  `VERSION` 으로 확인하지 못하면 멈춘다.
- `CONTRIBUTING.md` 와 이 문서를 한국어로. 사람이 읽는 문서다.
- README 에 "기존 파일을 건드리나" 절 — 쓰기·삭제·수정 경로를 전수 정리했다.

## 0.5.0

**작업자 확인이 지시가 아니라 강제가 됐다.**

- 세션 시작 훅은 매핑에 없는 이메일이면 "누구십니까"를 주입했지만, `PreToolUse` 에는 확정
  여부를 보는 검사가 아예 없었다. 모델에게 준 지시일 뿐이라 실제로는 아무것도 막히지 않았다.
  원본 규칙은 "확인 전에는 어떤 작업도 진행하지 않는다" 였으므로 구현 누락이었다.
- 이제 병행 모드에서 작업자가 확정되지 않으면 **문서 쓰기·커밋·push 가 차단**된다.
  "확정"은 `workers` 에 매핑이 생기는 것으로 정의했다 — 세션 안에만 있는 상태가 아니라
  언제든 확인 가능한 사실이어야 하기 때문이다.
- 막지 않는 것: 코드 편집(기록이 아니다), `girok.json` 자체(답을 적어야 한다),
  `SAFETY_GATE.md`(안전 정보는 기다리지 않는다). 셋 중 하나라도 막으면 세션 안에서 상황을
  풀 방법이 사라진다.

## 0.4.2

- README 를 한국어로 다시 쓰고 mermaid 다이어그램 두 개를 넣었다. 렌더링은 로컬
  mermaid-cli 로 확인했고, 목차 앵커와 표는 이 프로젝트 자신의 검사기로 검증했다.

## 0.4.1

- 유출 스캔이 자기가 스캔하는 폴더 안에 결과 파일을 써서 grep 이 "input file is also the
  output" 으로 exit 2 를 냈다. 0.4.0 에서 넣은 가드가 그걸 잡아 조용히 통과하지 않고
  실패시켰다 — 그 가드를 넣은 이유가 정확히 이것이다.

## 0.4.0

**공개 저장소로서의 모양새와 매니페스트.**

- `plugin.json` 에 `displayName`, 작성자 URL, 문서 `homepage`, 그리고 "무엇을 하는지"만이
  아니라 "무엇인지"를 말하는 키워드를 넣었다.
- `marketplace.json` 의 죽은 `$schema` 링크를 뺐다. Anthropic 이 이 파일들에 대해 공개하는
  스키마 URL 두 개가 **둘 다 404** 라서, 검증이 일어나지 않는데 일어나는 것처럼 보이게
  만드는 줄이었다.
- README 재작성, 한국어 사용법 문서, 이 변경 내역, 기여 안내.
- **한 번도 실제로 돈 적 없던 검사를 고쳤다**: 유출 스캔이 `grep -E` 와 `-P` 를 같이 넘겨
  `conflicting matchers specified` 로 exit 2 를 냈고, 그 비영 종료가 "일치 없음"으로 읽혀
  초록불이 됐다. 이제 스캔 자체가 실패하면 작업을 실패시키고, 카나리아 스텝이 정규식이
  여전히 뭔가를 잡는지 확인한다 — 실패하는 걸 아무도 볼 수 없는 초록불은 검사가 없는 것보다
  나쁘다.

## 0.3.1

- `modules.archive`. 두 번째 저장소에 도입했더니 거기에 `docs/archive/` 를 만들었는데, 그
  저장소의 규칙에 이렇게 적혀 있었다 — "아카이브 폴더를 만들지 않는다, 지운 건 git
  히스토리에 있다". 초대받은 저장소의 관례를 플러그인이 깨면 권한 방향이 거꾸로다.

## 0.3.0

**저장소 레이아웃이 설정이 됐다.**

- 새 키: `board`, `decisionsDir`, `docRoots`, `rootDocs`, `rulesDocs`, `adrStyle`.
  두 번째로 도입한 저장소는 `STATE.md` 와 `decisions/NNN-slug.md` 를 루트에 둔다. 결정 파일
  52개를 리네임하는 비용이 얻는 것보다 컸고, 자기가 나온 저장소에만 맞는 방법론은 결국
  단계만 늘어난 사본이다.
- `adrStyle: "numbered"` 는 맨숫자를 인용으로 치지 않는다 — 본문의 모든 수치가 ID처럼
  보여서 오탐이 진짜 발견을 묻어버린다. 인덱스 등재 검사는 그대로 돌고, 결정이 52건이면
  그게 값이 나온다.
- **고침:** `marker_scan` 이 notes 트리 전체를 제외했는데 `notesDir: "."` 이면 저장소
  전체다 — 스캔이 조용히 항상 아무것도 못 찾았을 것이다.
- **고침:** `notes_init` 이 기존 설정을 무시해서, 그런 저장소에서 `/notes` 를 치면 진짜
  문서 옆에 빈 뼈대를 하나 더 뿌렸을 것이다.

## 0.2.2

- 사용법 문서. 그리고 README 를, 가정한 설치 절차가 아니라 **리허설에서 실제로 필요했던**
  절차로 고쳤다.

## 0.2.1

- 문서 수정.

## 0.2.0

**첫 동작 릴리스:** 스킬 6종, 훅 5종, 검사기 3종, 템플릿, CI.

샌드박스 저장소에서 전 과정을 리허설했다 — 세션 시작 마커가 실제로 주입되고, 원점복귀를
언급하면 열린 게이트 항목이 뜨고, 손댄 스냅샷이 무결성 검사에서 걸리고, 미등재
`SAFETY-STUB` 이 커밋을 막는다.

그 리허설에서 고친 것들. 하나하나가 어떤 환경에서 플러그인을 **조용히 무용지물로** 만들고
있었다.

- Windows 의 `python3` 는 보통 Microsoft Store 앱 실행 앨리어스로 잡힌다 — 인터프리터가
  아니라 `Python` 을 찍고 exit 49 를 낸다. 래퍼가 그걸 골라서, Python 이 깔린 머신에서 훅이
  전부 조용히 죽었다. 이제 인터프리터를 실행해서 확인한다.
- `*.cmd eol=crlf` 가 폴리글롯 래퍼의 유닉스 절반이 실행하는 모든 명령에 `\r` 을 넣었다.
- 스냅샷의 스크립트를 실행하면 옆에 `__pycache__` 가 생겨 내용 해시가 바뀌었다 — CI 게이트가
  자기 부작용으로 실패했다.
- `method_sync.py` 가 스냅샷에 없어서, CI 가 호출하는 파일이 존재할 수 없었다.
- 검사기의 슬러그가 공백을 뭉쳐서, 규칙이 경고하는 바로 그 실수를 잡지 못했다 — 공백으로
  감싼 구분 문자가 들어간 헤더는 GitHub 앵커가 하이픈 둘인데 손으로 쓴 목차는 하나다.
- 한국어 Windows 콘솔에서 출력이 `UnicodeEncodeError` 로 죽었다. 검사기 자신의 메시지에
  em dash 가 들어 있어서였다.
- CI 워크플로가 `main` 을 감시했는데 브랜치는 `master` 였다. 한 번도 실행된 적이 없었다.

## 0.1.0

플러그인 뼈대. 저장소에 커밋된 `.claude/settings.json` 의 마켓플레이스 선언이 폴더 신뢰
승인 후 자동 등록되는 것을 확인했다 — 그리고 **승인 전에는 아무 일도 일어나지 않는다는
것도.** 그래서 `CLAUDE.md` 부트스트랩 게이트가 존재한다.
